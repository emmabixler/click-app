import React from "react";
import "../header.css";

const header = () => (
  <div className="header">
    <div>
      Click on the image, for points but don't pick the same image or you will
      lose.
    </div>
  </div>
);

export default header;
